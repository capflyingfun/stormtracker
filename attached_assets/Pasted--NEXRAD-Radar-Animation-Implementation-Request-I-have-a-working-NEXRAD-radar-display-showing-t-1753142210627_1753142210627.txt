# NEXRAD Radar Animation Implementation

## Request

I have a working NEXRAD radar display showing the latest frame. I need you to add animation functionality to show the last 2 hours of radar data as a smooth animated loop.

## Current Setup

- Using Leaflet.js map
- Loading NEXRAD data from Iowa Environmental Mesonet
- Currently showing single static radar frame
- Need to convert to animated time series

## Animation Requirements

### Data Fetching

- Generate timestamps for last 2 hours (24 frames, 5-minute intervals)
- Use Iowa Mesonet URL format: `https://mesonet.agron.iastate.edu/archive/data/YYYY/MM/DD/GIS/uscomp/n0q_YYYYMMDDHHmm.png`
- Handle missing frames gracefully
- Preload frames for smooth playback

### Animation Controls

- Play/Pause button
- Speed control (0.5x, 1x, 2x speeds)
- Timeline scrubber for manual frame selection
- Current timestamp display
- Loop on/off toggle

### Technical Implementation

- Use `L.imageOverlay()` for each radar frame
- Animate by adding/removing layers in sequence
- Frame rate: ~400ms between frames for smooth viewing
- Clean up previous layers to prevent memory leaks
- Use `requestAnimationFrame` or `setInterval` for timing

## Code Structure Needed

```javascript
// 1. Timestamp generation function
function generateTimestamps() {
    // Return array of timestamps for last 2 hours
}

// 2. Frame loading function  
async function loadRadarFrames() {
    // Create L.imageOverlay for each timestamp
    // Store in array with metadata
}

// 3. Animation controller
class RadarAnimation {
    constructor() {
        this.frames = [];
        this.currentFrame = 0;
        this.isPlaying = false;
        this.speed = 1;
    }
    
    play() { /* start animation loop */ }
    pause() { /* stop animation */ }
    setFrame(index) { /* jump to specific frame */ }
    setSpeed(multiplier) { /* adjust playback speed */ }
}

// 4. UI controls
function createAnimationControls() {
    // Generate HTML for play/pause/speed/timeline
    // Attach event handlers
}
```

## Error Handling Requirements

- Skip missing radar frames without breaking animation
- Show loading indicator while fetching frames
- Display helpful error messages for network issues
- Gracefully handle slow connections

## UI Elements Needed

- Play/pause button with appropriate icons
- Speed selector (0.5x, 1x, 2x buttons or dropdown)
- Timeline slider showing frame position
- Timestamp display (formatted as “MM/DD HH:MM”)
- Loading spinner for frame loading
- Simple CSS styling for controls overlay

## Bounds Configuration

Use these geographic bounds for NEXRAD composite overlay:

```javascript
const bounds = [
    [20.0, -130.0], // Southwest corner  
    [50.0, -60.0]   // Northeast corner
];
```

## Performance Considerations

- Preload 3-5 frames ahead of current position
- Remove old frames from map when not visible
- Use efficient DOM manipulation
- Handle memory cleanup on page unload

Please provide:

1. Complete JavaScript code for the animation system
1. HTML for the control interface
1. CSS for styling the controls overlay
1. Integration instructions for adding to existing map

Focus on reliability and smooth playback - the animation should work consistently without stuttering or memory issues during extended use.