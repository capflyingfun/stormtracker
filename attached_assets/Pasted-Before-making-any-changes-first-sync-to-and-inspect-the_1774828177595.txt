Before making any changes, first sync to and inspect the newest code in the current working branch/repo, especially the latest versions of:
- client/src/pages/*
- client/src/components/*
- client/src/hooks/*
- client/src/lib/*
- any layout, shell, modal, sidebar, or settings-related files

Do not assume older cached context. Read the current files first and summarize:
1. which files control the main app layout
2. which files control responsive breakpoints
3. which files control the settings modal sizing/layout
4. which files control weather/radar/storm page panel widths and spacing

Then improve StormTracker’s desktop landscape layout for common screens like 1920x1080 while preserving the existing mobile portrait behavior.

Goals:
- Keep the current visual style, colors, glow, and overall aesthetic
- Do NOT redesign the app from scratch
- Do NOT break mobile layout
- Make desktop use space better and feel intentional
- Keep sidebar/navigation clean and proportional
- Improve spacing, panel widths, and max-width behavior
- Make the settings modal desktop-friendly instead of narrow/mobile-styled
- Use responsive breakpoints properly for desktop, tablet, and phone
- Avoid oversized empty space on wide screens
- Ensure cards, charts, radar, and weather sections scale appropriately on desktop
- Preserve current functionality and component structure as much as possible

Specific issues to fix:
- The main content area still feels like a phone/tablet layout stretched onto desktop
- The settings panel/modal remains too narrow and tall on desktop
- Desktop sections should use wider multi-column layouts where appropriate
- Weather overview, wind/trends, radar, and forecast areas should feel balanced on widescreen
- Keep alerts/header/top bar aligned cleanly across desktop widths
- Avoid making text or controls too tiny

Requirements:
- Read the newest code first before editing
- Explain which files you changed and why
- Prefer targeted edits over broad rewrites
- If layout logic is split across multiple components, update all relevant ones consistently
- If there are existing utility classes or design tokens, reuse them
- After changes, verify desktop landscape, tablet, and mobile behavior
- Tell me if any layout issue comes from a shared container, modal wrapper, or breakpoint utility

Please start by auditing the current layout files first, then make the responsive improvements.