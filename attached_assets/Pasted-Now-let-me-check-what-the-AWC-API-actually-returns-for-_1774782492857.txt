Now let me check what the AWC API actually returns for KPNS and KMOB to understand the data issue:​​​​​​​​​​​​​​​​

Now I see the issue. The AWC API had parameter and response schema changes in September 2025. ￼ Let me look closely at the parseAWCobs function and the NWS observation path to find the wind bug:
The problem is clear now. There are two separate issues causing missing wind data at KPNS/KMOB:
Issue 1: NWS API path — wind data extraction from properties vs raw METAR
In loadStationObs(), when the NWS API returns a raw METAR (like KPNS 010353Z AUTO 01008KT 10SM OVC100 11/04 A3017...), the code correctly calls parseRawMetar() which parses 01008KT into wind direction 010° and speed 8 knots. That works fine.
BUT — when the NWS API returns observation data without a rawMessage (which happens intermittently for KPNS and KMOB since they have $ maintenance flags), the fallback at line ~270 reads p.windSpeed.value directly. The NWS API returns wind speed in km/h in the windSpeed.value field, and the code stores it directly as windKmh: p.windSpeed.value — that’s correct. However, windDirection.value can sometimes be null when the NWS reports VRB or when their API has a data gap, causing the wind to display as “–” even though speed exists.
Issue 2: AWC API September 2025 schema change — the real culprit
The AWC API changed its response schema in September 2025. Looking at the parseAWCobs() function, it reads m.wspd and m.wdir — but after the schema change, the field names may have changed. The HawgOps example response shows the fields are still wspd, wdir, wgst — but the AWC API may now return wind data differently for some stations, or may return wdir as a string "VRB" which gets passed to Number() and becomes NaN.
Here’s the fix prompt:

Fix: Missing wind data for KPNS, KMOB, and other stations in docs/js/station.js
Bug 1: NWS fallback path loses wind when rawMessage is empty
In loadStationObs(), when the NWS API doesn’t return a rawMessage (common for stations with $ maintenance flags like KPNS and KMOB), the fallback block builds the station object directly from p.windSpeed.value etc. But p.windDirection.value can be null for variable winds, and when that happens the entire wind display shows “–” even though wind speed is available.
Fix the fallback block (around line where S.station is built without raw METAR) to handle null wind direction:

windKmh: p.windSpeed?.value != null ? p.windSpeed.value : null,
windDir: p.windDirection?.value != null ? p.windDirection.value : null,
gustKmh: p.windGust?.value != null ? p.windGust.value : null,


And in renderStation(), update the wind display so it shows the speed even when direction is null/VRB:

<div style="font-size:1.4em;font-weight:700">
  ${windKmh != null ? fmtWind(windKmh) : (gustKmh != null ? fmtWind(gustKmh) : 'Calm')}
</div>
<div class="c-muted-sm">
  ${wDir != null ? degToDir(wDir) + ' wind' : (windKmh != null ? 'Variable wind' : (gustKmh != null ? 'Gusting' : 'Calm'))}
</div>


Currently the code shows “Calm” if windKmh is null, but it should check if the raw METAR has wind data even when the parsed field is missing.
Bug 2: parseAWCobs() doesn’t handle wdir: "VRB" safely
In parseAWCobs(), line windDir: m.wdir != null ? (m.wdir === 'VRB' ? null : Number(m.wdir)) : null — this looks correct, BUT m.wdir might come as the number 0 (true north / calm) which is falsy. The !=null check handles this, but also verify that Number(m.wdir) doesn’t produce NaN for unexpected string values. Add a safety check:

windDir: m.wdir != null ? (m.wdir === 'VRB' || m.wdir === '' ? null : (isNaN(Number(m.wdir)) ? null : Number(m.wdir))) : null,


Bug 3: parseRawMetar() doesn’t handle 00000KT (calm winds) distinctly
When METAR reports 00000KT (calm), the regex ^(\d{3}|VRB)(\d{2,3})(G(\d{2,3}))?KT$ matches with direction=000 and speed=0. This is technically correct but displays as “N wind at 0 mph” instead of “Calm”. Add after the wind regex match:

if (windKt === 0 && (windDir === 0 || windDir === null)) {
  windDir = null; // Calm — no meaningful direction
}


Bug 4: NWS API sometimes returns stale/empty observations
For KPNS and KMOB specifically, the NWS /stations/{ICAO}/observations/latest endpoint sometimes returns observations with null values for wind, temp, and other fields — essentially an empty observation shell. When this happens, the code should fall through to the AWC METAR API instead of displaying “–” for everything.
In loadStationObs(), after parsing the NWS response, add a quality check before accepting it:

// After building S.station from NWS data, check if it's actually useful
const hasUsableData = S.station.temp != null || S.station.windKmh != null || S.station.visMeter != null;
if (!hasUsableData) {
  console.log('NWS returned empty observation for', icao, '— falling back to AWC');
  throw new Error('NWS observation empty');
}


This ensures that if NWS returns a hollow observation (which happens for KPNS/KMOB during maintenance windows indicated by the $ remark), the code falls through to loadStationObsAWC() which fetches directly from the AWC METAR API and typically has the complete data.

The core issue is that KPNS and KMOB both have $ in their METAR remarks (meaning the station needs maintenance), and the NWS API sometimes returns degraded or incomplete observations for these stations. The fix ensures the code falls through to AWC when NWS data is hollow, handles VRB/calm winds properly, and doesn’t show “–” when wind speed exists but direction is variable.​​​​​​​​​​​​​​​​