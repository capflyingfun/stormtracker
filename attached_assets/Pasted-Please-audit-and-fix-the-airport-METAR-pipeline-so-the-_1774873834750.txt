Please audit and fix the airport METAR pipeline so the app shows the official raw METAR from AviationWeather.gov as closely as possible.

Problem:
The app’s KPNS and KMOB METARs do not match the official current METARs from aviationweather.gov. The app appears to be reconstructing a METAR string from parsed fields instead of preserving the official raw report.

Examples:
Official:
KPNS 301153Z AUTO 08007KT 10SM OVC018 16/14 A3027 RMK AO2 SLP252 T01610139 10161 20156 53004
KMOB 301156Z 09006KT 7SM BKN015 16/14 A3026 RMK AO2 SLP246 T01610139 10167 20150 53002

App:
KPNS 301200Z AUTO 08007KT 10SM OVC018 16/14 A3027 RMK AO2 SLP251 T01600140
KMOB 301200Z AUTO 09006KT 7.0SM OVC023 16/14 A3026 RMK AO2 SLP247 T01600140

What I want:
1. Read the latest current code first. Do not rely on memory.
2. Find the full METAR data flow for the Station/Airport page, including:
   - loadStationObsAWC
   - parseAWCobs
   - renderStation
   - any helper that formats or rebuilds METAR strings
3. Determine whether the app is reconstructing METAR text from parsed fields.
4. Change the pipeline so the app prefers the official AWC raw METAR text from the API response whenever available.
5. Preserve parsed fields for cards/decoded display, but do NOT regenerate the raw METAR if official raw text exists.
6. If the JSON endpoint does not currently expose the raw text as expected in this code path, update the fetch/parser so the raw METAR is captured from the AWC response in a reliable way.
7. Make sure timestamps, cloud groups, visibility formatting, SLP, and precise temp/dewpoint remarks come from the official report, not rebuilt approximations.
8. Keep fallback behavior only if raw METAR is truly unavailable.

Important:
- Audit the newest code before editing.
- Tell me exactly which files control METAR fetching, parsing, and rendering.
- Explain whether parseAWCobs is dropping raw_text or whether another function is rebuilding the string.
- Use targeted edits, not a broad rewrite.
- After changes, verify KPNS and KMOB against current AWC METAR output formatting.
- Preserve existing UI and decoded weather cards.

Implementation guidance:
- Prefer storing something like rawMETAR directly from the AWC response object.
- If needed, map AWC raw_text into S.station.rawMETAR during parseAWCobs.
- Avoid top-of-hour synthetic timestamps like 301200Z if the source observation is 301153Z or 301156Z.
- Avoid reformatting visibility like 7SM into 7.0SM in the raw METAR display.
- Avoid deriving sky condition groups from cloud arrays for the raw METAR line.
- Use derived/reformatted data only for decoded summaries, never for the official raw METAR line.

Please start by identifying the exact files/functions involved, then patch only the necessary code.