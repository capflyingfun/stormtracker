There they are — just pushed recently! docs/js/app.js, docs/css/style.css, and docs/index.html (plus a root index.html).​​​​​​​​​​​​​​​​

Now let me dig into the app.js — specifically the NEXRAD radar animation code:​​​​​​​​​​​​​​​​

Now I can see the bug clearly. Let me verify one more detail:​​​​​​​​​​​​​​​​

Let me verify what format the Iowa Mesonet actually expects:​​​​​​​​​​​​​​​​

Now I have the full picture. The Iowa Mesonet tile URL format for historical NEXRAD composites is very specific. Let me verify the exact layer naming:​​​​​​​​​​​​​​​​

Now I have the definitive answer. Here’s the bug:​​​​​​​​​​​​​​​​

Found it. Here’s your fix prompt, Joshua:

Bug: NEXRAD radar animation shows only the first frame — all other frames are black/empty

Root cause is in docs/js/app.js, function buildNexradFrames() (line 1762).

The function constructs tile URLs using this pattern:

nexrad-n0q-${ts}-900913/{z}/{x}/{y}.png


Where ts is a fabricated timestamp like 202603192200. This layer name format does not exist on the Iowa Mesonet tile server. According to Iowa Mesonet’s own documentation (https://mesonet.agron.iastate.edu/ogc/), the valid NEXRAD composite tile layers are:

	∙	nexrad-n0q-900913 — the current/latest composite (this is what showRadarLayer uses on line 1857, and why the first frame works)
	∙	ridge::SITE-N0Q-YYYYMMDDHHMM — archived per-site imagery with a specific format

There is no nexrad-n0q-YYYYMMDDHHMM-900913 layer. So all 25 frames built by buildNexradFrames() use invalid URLs. The very first frame might appear to work because it’s close enough in time that the server might return the current composite, or the browser is still showing the previously loaded current layer — but frames 2-24 all 404 silently and Leaflet renders them as black/transparent tiles.

Fix: Replace the fabricated-timestamp approach with one of these two options:

Option A (Recommended — use the NEXRAD composite WMS-T service for historical frames):
The Iowa Mesonet provides a time-based WMS service for historical NEXRAD composites at:

https://mesonet.agron.iastate.edu/cgi-bin/wms/nexrad/n0q-t.cgi


Use Leaflet’s L.tileLayer.wms() with a TIME parameter in ISO 8601 format (2026-03-19T22:00:00Z). This returns the actual archived composite for that time.

Replace buildNexradFrames() with:

async function buildNexradFrames(){
  const frames=[];
  const now=Date.now();
  for(let i=24;i>=0;i--){
    const t=new Date(now - i*5*60000);
    const isoTime = t.toISOString().replace(/\.\d{3}Z$/,'Z');
    frames.push({
      time: Math.floor(t.getTime()/1000),
      type: 'past',
      isoTime,
      wms: true
    });
  }
  return frames;
}


And update showRadarAnimFrame() (line 1832) to handle WMS frames:

if(frame.wms){
  S.radarLayer = L.tileLayer.wms(
    'https://mesonet.agron.iastate.edu/cgi-bin/wms/nexrad/n0q-t.cgi',
    { layers:'nexrad-n0q-wmst', transparent:true, format:'image/png',
      time: frame.isoTime, opacity:0.7 }
  ).addTo(map);
} else {
  // existing RainViewer tile logic
  S.radarLayer = L.tileLayer(frame.url, {opacity:0.7, maxZoom:11, maxNativeZoom:maxNZ}).addTo(map);
}


Option B (Simpler fallback — use per-site RIDGE tiles with real timestamps):
Fetch actual available scan times from the JSON API:

https://mesonet.agron.iastate.edu/json/radar.py?operation=list&radar=MOB&product=N0Q


Then build tile URLs using the documented ridge:: format:

https://mesonet.agron.iastate.edu/cache/tile.py/1.0.0/ridge::MOB-N0Q-YYYYMMDDHHMM/{z}/{x}/{y}.png


This requires knowing the nearest radar site (you can use the existing commonRadarSites lookup from the server code, or hardcode a simpler version client-side).

Summary: The layer name nexrad-n0q-TIMESTAMP-900913 is fabricated and doesn’t exist on the server. Use either the WMS-T service (Option A) or the ridge:: SITE-specific tiles with real timestamps from the JSON API (Option B).​​​​​​​​​​​​​​​​