Yes! Here’s the core problem — and it’s a big one:

The NEXRAD animation is fundamentally broken because the client never fetches historical NEXRAD timestamps.

Here’s what’s happening:

When radarSource is set to 'nexrad' (line 222-288 of storm-map.tsx), the code sets radarFrames to just ['current'] — a single-element array with the literal string "current". That means:

	1.	The slider has only 1 frame to work with, so it “moves” but shows nothing new
	2.	startAnimation() at line 296 bails out immediately because radarFrames.length < 2
	3.	When the frame update effect runs (line 331), it hits the NEXRAD branch (line 350-361) and checks timestampStr.startsWith('current'), which is always true since 'current' is the only frame — so it just reloads the same live composite tile every time

Meanwhile, the server does have an endpoint at /api/nexrad/timestamps/:site (line 2498) that fetches real historical timestamps from Iowa Mesonet — but the client never calls it.

Compare this to RainViewer, which correctly fetches multiple past timestamps from /api/rainviewer and loads them into radarFrames as an array of Unix timestamps.

Here’s your fix prompt for Replit:

Bug: NEXRAD radar animation only shows first frame — remaining frames are black/empty
Root cause: In client/src/components/storm-map.tsx, the NEXRAD initialization (around line 244) hardcodes radarFrames to ['current'] — a single-element array. The client never calls the existing /api/nexrad/timestamps/:site server endpoint to get historical frame timestamps. This means:
	∙	The animation slider has only 1 frame, so startAnimation() exits early at the radarFrames.length < 2 check (line 297)
	∙	The frame update effect (line 331) always sees the string 'current' and reloads the same live composite tile
	∙	All other slider positions show black/empty because there are no real timestamps to request tiles for
Fix needed in client/src/components/storm-map.tsx, in the loadRadarFrames function’s NEXRAD branch (around lines 223-288):
After finding the nearest radar site and setting nexradSite, fetch real historical timestamps from the server:

const timestampResponse = await fetch(`/api/nexrad/timestamps/${site}`);
if (timestampResponse.ok) {
  const tsData = await timestampResponse.json();
  if (tsData.timestamps && tsData.timestamps.length > 0) {
    setRadarFrames(tsData.timestamps);
    setCurrentFrame(Math.max(0, tsData.timestamps.length - 1));
    setCurrentFrameIndex(tsData.timestamps.length - 1);
  }
}


Only fall back to ['current'] if the timestamps endpoint fails or returns empty data.
Additionally, there is a secondary bug in the NEXRAD tile proxy at server/routes.ts line 2557. The URL format ridge::${site}::${timestamp} may not match what the Iowa Mesonet API actually returns for timestamp format. Verify that the timestamps returned from /api/nexrad/timestamps/:site (which come from data.scans or data.times) are in the correct format for the RIDGE tile URL pattern ridge::{site}::{timestamp}. Log the actual timestamp values and test the constructed tile URL to confirm it resolves to real imagery. If the format is wrong, the tile requests will 404 and silently fall back to the current composite (line 2562), which would cause all frames to look identical.

In short: the server-side plumbing exists but the client-side wiring was never connected. RainViewer works because it actually fetches multiple timestamps — NEXRAD just gets ['current'] and calls it a day.​​​​​​​​​​​​​​​​